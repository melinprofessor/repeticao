// primeiro criar se a variavel
// segundo criar se a condição
// terceiro o incremento para a variavel
// o for na primeria repetição não incrementa
for(let x=0; x <= 9; x++){
    console.log(x);
}

for (let x = 0; x <= 10; x++) {
   for(let y = 0; y <= 10; y++)
   {
    console.log(`${x} x ${y} = ${x * y}`)
   }
}